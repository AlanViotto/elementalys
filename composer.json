{
    "name": "elementalys/control-system",
    "description": "Sistema de controle Elementalys com cadastro de clientes, produtos, fornecedores e vendas.",
    "type": "project",
    "require": {
        "php": ">=8.1"
    },
    "autoload": {
        "psr-4": {
            "Elementalys\\": "src/Elementalys/"
        }
    },
    "scripts": {
        "post-autoload-dump": [
            "@php -r \"file_exists('public/index.php') || mkdir('public', 0777, true);\""
        ]
    }
}
